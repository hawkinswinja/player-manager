pipeline {
    agent any
    stages {
        stage('checkout') {
            steps {
                sh 'ls'
            }
        }

        stage('Deploy') {
            steps {
                sh 'ls'
            }
        }
    }
}
